extends -page-layout.pug

block prepend variables
  block houseVariables

block main
  main.main
    include widget-headset.pug

    section(class= `house-promo house-promo--pending house-promo${classModifier}`)
      .wrapper.wrapper--ext
        .house-promo__text
          h1.house-promo__title= houseData["header"][0]
          .house-promo__title-descr= houseData["header"][1]
          //- p.house-promo__price= `${houseData["price"]} ₽`
          p.house-promo__status Проект в разработке
          p.house-promo__soon-block Уже скоро!
        ul.house-promo__images-list
          li.house-promo__images-item
            +pictureJpgWebp(
              houseData["picturesFiles"][0]["name"],
              houseData["picturesFiles"][0]["dimensions"][0],
              houseData["picturesFiles"][0]["dimensions"][1],
              houseData["picturesAltText"],
              "house-promo__img0"
            )
        .house-promo__slider
          .swiper-dummy
            .house-promo__slider-list
              each item, index in houseData["sliderData"]["files"]
                .house-promo__slider-item
                  +pictureJpgWebp(item, houseData["sliderData"]["dimensions"][0], houseData["sliderData"]["dimensions"][1], houseData["picturesAltText"], "house-promo__slider-img")
